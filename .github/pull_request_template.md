## What

Brief description of the change.

## Why

Why is this change needed?

## Testing

- [ ] `go test ./...` passes
- [ ] `go vet ./...` clean
- [ ] Tested manually with `claude-config export` / `import` / `inspect`
